{
  "buildCommand": "cd ../.. && npm run build --workspace=@sync-erp/web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "cd ../.. && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
