# API Contracts

This feature is **Internal Backend Automation** triggered by Service calls.
No new public HTTP API endpoints are exposed to the Frontend.

The integration is between `InventoryService` and `JournalService`.
