name: Deploy Bot to Hostinger

on:
  push:
    branches: [main, dev]
    paths:
      - '.github/workflows/deploy-bot-hostinger.yml'
      - 'apps/bot/**'
      - 'packages/shared/**'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production

permissions:
  contents: read

concurrency:
  group: deploy-bot-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/bot

    env:
      DEPLOY_ENV: ${{ github.event.inputs.environment || (github.ref == 'refs/heads/main' && 'production' || 'staging') }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Resolve deployment target
        id: vars
        run: |
          if [[ "${{ env.DEPLOY_ENV }}" == "production" ]]; then
            echo "deploy_path=apps/bot" >> $GITHUB_OUTPUT
            echo "health_host=https://bot.sync-erp.com" >> $GITHUB_OUTPUT
          else
            echo "deploy_path=apps/bot-staging" >> $GITHUB_OUTPUT
            echo "health_host=https://bot-staging.sync-erp.com" >> $GITHUB_OUTPUT
          fi
          echo "Deploying to: ${{ env.DEPLOY_ENV }}"

      - name: Install dependencies
        run: npm ci
        working-directory: .

      - name: Clean Prisma generated files
        run: rm -rf packages/database/src/generated packages/shared/src/generated node_modules/.prisma
        working-directory: .

      - name: Generate Prisma Client
        run: npm run db:generate
        working-directory: packages/database

      - name: Build Bot
        run: npm run build:bot
        working-directory: .

      - name: Prepare deployment package
        run: |
          rm -rf deploy
          mkdir -p deploy/deploy/apps/bot
          cp -r apps/bot/dist deploy/deploy/apps/bot/
          cp apps/bot/package.pro.json deploy/deploy/apps/bot/package.json
          mkdir -p deploy/deploy/prisma
          cp packages/database/prisma/schema.prisma deploy/deploy/prisma/
          cp -R packages/database/dist deploy/deploy/node_modules/@sync-erp/database/
          cp packages/database/package.json deploy/deploy/node_modules/@sync-erp/database/

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.HOSTINGER_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -p 65002 ${{ secrets.HOSTINGER_HOST }} > ~/.ssh/known_hosts 2>/dev/null || true

      - name: Deploy to Hostinger
        env:
          SSH_AUTH_SOCK: /tmp/ssh_agent.sock
          SSH_PASSPHRASE: ${{ secrets.HOSTINGER_SSH_PASSPHRASE }}
          DEPLOY_DIR: ${{ steps.vars.outputs.deploy_path }}
        run: |
          eval $(ssh-agent -a $SSH_AUTH_SOCK)
          echo 'echo "$SSH_PASSPHRASE"' > ~/.ssh/askpass
          chmod +x ~/.ssh/askpass
          export SSH_ASKPASS=~/.ssh/askpass
          export DISPLAY=:0
          ssh-add ~/.ssh/id_ed25519

          SSH_OPTS="-p 65002 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ServerAliveInterval=30 -o ServerAliveCountMax=10"
          BOT_ARCHIVE="$(mktemp)"
          REMOTE_BOT_ARCHIVE="sync-erp-bot-${GITHUB_SHA}.tar.gz"

          tar -C deploy \
            --exclude='./.htaccess' \
            --exclude='./.env' \
            --exclude='./tmp' \
            --exclude='./stderr.log' \
            -czf "$BOT_ARCHIVE" .

          timeout 600 scp -C -P 65002 ${SSH_OPTS} \
            "$BOT_ARCHIVE" \
            "${{ secrets.HOSTINGER_USER }}@${{ secrets.HOSTINGER_HOST }}:~/${REMOTE_BOT_ARCHIVE}"

          ssh ${SSH_OPTS} "${{ secrets.HOSTINGER_USER }}@${{ secrets.HOSTINGER_HOST }}" "DEPLOY_DIR='${DEPLOY_DIR}' REMOTE_BOT_ARCHIVE='${REMOTE_BOT_ARCHIVE}' bash -s" <<'EOF'
            set -euo pipefail
            export PATH=/opt/alt/alt-nodejs20/root/usr/bin:$PATH
            target="$HOME/public_html/${DEPLOY_DIR}"

            mkdir -p "$target"
            find "$target" -mindepth 1 -maxdepth 1 \
              ! -name '.env' \
              ! -name '.htaccess' \
              ! -name 'node_modules' \
              ! -name 'tmp' \
              ! -name 'stderr.log' \
              -exec rm -rf {} +
            tar -xzf "$HOME/${REMOTE_BOT_ARCHIVE}" -C "$target"
            cd "$target"

            fingerprint="$(sha256sum package.json | awk '{print $1}')"
            installed_fingerprint=""
            if [ -f .package-deps.sha256 ]; then
              installed_fingerprint="$(cat .package-deps.sha256)"
            fi

            if [ -d node_modules ] && [ -f node_modules/telegraf/package.json ] && { [ -z "$installed_fingerprint" ] || [ "$installed_fingerprint" = "$fingerprint" ]; }; then
              echo "Reusing existing production node_modules."
              echo "$fingerprint" > .package-deps.sha256
            else
              echo "Installing production dependencies."
              timeout 600 npm install --omit=dev --ignore-scripts --no-audit --no-fund --loglevel=warn
              echo "$fingerprint" > .package-deps.sha256
            fi

            rm -f "$HOME/${REMOTE_BOT_ARCHIVE}"
            mkdir -p tmp
          EOF

          rm -f "$BOT_ARCHIVE"

          ssh ${SSH_OPTS} "${{ secrets.HOSTINGER_USER }}@${{ secrets.HOSTINGER_HOST }}" \
            "cd ~/public_html/${DEPLOY_DIR} && mkdir -p tmp && touch tmp/restart.txt"
